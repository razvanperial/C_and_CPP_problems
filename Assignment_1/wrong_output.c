#include <stdio.h>
int main() {
   int result;
   result = (2 + 7) * 9 / 3; /* The result of our calculation */
   printf("The result is %d\n", result);/*I added the result argument 
   in the printf function in order to show the result on screen. 
   The program printed a wrong value because no parameter was passed
   in the printf function */
   return 0;
}
