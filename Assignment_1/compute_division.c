#include <stdio.h>

int main(){
   float result;  /* The result of our calculation */
   result = ((float)(3+1)) / 5;/*I transformed one of the parameters into a 
   float in order to make the result of the division a float*/
   printf("The value of 4/5 is %f\n", result);
   return 0;
}
